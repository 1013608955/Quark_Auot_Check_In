name: Quark签到

on:
  schedule:
    - cron: '0 1 * * *'   # 北京时间 09:00 (UTC+8)
    - cron: '0 5 * * *'   # 北京时间 13:00 (UTC+8)
  workflow_dispatch:      # 允许手动触发

jobs:
  check-if-already-signed:
    runs-on: ubuntu-latest
    outputs:
      skip: ${{ steps.check.outputs.skip }}
    steps:
      - name: 判断是否已签到（读取仓库变量）
        id: check
        run: |
          # 获取当前北京时间（格式：2026-01-02）
          current_date=$(TZ='Asia/Shanghai' date '+%Y-%m-%d')
          echo "当前北京时间: $current_date"
          
          # 读取仓库变量中存储的上次签到日期（无则为空）
          last_success_date="${{ vars.LAST_SUCCESS_DATE || '' }}"
          echo "上次签到日期: $last_success_date"
          
          # 判断是否已签到
          if [ "$last_success_date" = "$current_date" ]; then
            echo "✅ 今天已成功签到，跳过执行"
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "🔄 今天尚未签到，继续执行"
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

  sign-in:
    needs: check-if-already-signed
    if: needs.check-if-already-signed.outputs.skip == 'false'
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v3

      - name: 随机延迟（早上）
        if: github.event.schedule == '0 1 * * *'
        run: |
          delay=$((RANDOM % 60))
          echo "早上签到，延迟 $delay 秒后开始.."
          sleep $delay

      - name: 随机延迟（下午/手动触发）
        if: github.event.schedule == '0 5 * * *' || github.event_name == 'workflow_dispatch'
        run: |
          delay=$((RANDOM % 30))
          echo "下午兜底或手动触发，延迟 $delay 秒后开始.."
          sleep $delay

      - name: 设置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: 安装依赖
        run: pip install requests

      - name: 执行签到脚本
        id: sign_script  # 命名步骤，用于读取输出
        env:
          COOKIE_QUARK: ${{ secrets.COOKIE_QUARK }}
          WPUSH_KEY: ${{ secrets.WPUSH_KEY }}
        run: python checkIn_Quark.py

      - name: 标记今天签到成功（更新仓库变量）
        if: steps.sign_script.outputs.overall_success == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            // 生成北京时间格式的日期（2026-01-02）
            const currentDate = new Date().toLocaleDateString('zh-CN', { 
              timeZone: 'Asia/Shanghai' 
            }).replace(/\//g, '-');
            
            // 更新仓库变量 LAST_SUCCESS_DATE
            await github.rest.repos.updateRepoVariable({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'LAST_SUCCESS_DATE',
              value: currentDate
            });
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: 清理旧的工作流记录
        uses: Mattraks/delete-workflow-runs@main
        if: always()
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 60
