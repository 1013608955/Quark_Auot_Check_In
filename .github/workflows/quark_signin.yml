name: Quark签到

on:
  schedule:
    - cron: '0 1 * * *'   # 北京时间 09:00 (UTC+8)
    - cron: '0 5 * * *'   # 北京时间 13:00 (UTC+8)
  workflow_dispatch:

jobs:
  check-if-already-signed:
    runs-on: ubuntu-latest
    outputs:
      skip: ${{ steps.check.outputs.skip }}
    steps:
      - name: 恢复签到状态缓存
        id: cache
        uses: actions/cache@v3
        with:
          path: .last_success_date
          key: ${{ runner.os }}-last-success-date-${{ github.ref }}
          restore-keys: ${{ runner.os }}-last-success-date-

      - name: 检查是否已签到
        id: check
        run: |
          # 获取当前北京时间（UTC+8）
          current_date=$(TZ='Asia/Shanghai' date '+%Y-%m-%d')
          echo "当前北京时间: $current_date"
          
          # 检查缓存文件是否存在且日期匹配
          skip="false"
          if [ -f .last_success_date ]; then
            last_date=$(cat .last_success_date)
            echo "上次成功签到日期: $last_date"
            if [ "$last_date" = "$current_date" ]; then
              echo "✅ 今天已成功签到，跳过执行。"
              skip="true"
            fi
          fi
          
          echo "skip=$skip" >> "$GITHUB_OUTPUT"
          if [ "$skip" = "false" ]; then
            echo "📅 今天尚未成功签到，继续执行。"
          fi

  sign-in:
    needs: check-if-already-signed
    if: needs.check-if-already-signed.outputs.skip == 'false'
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v3

      - name: 随机延迟（早上）
        if: github.event.schedule == '0 1 * * *'
        run: |
          delay=$((RANDOM % 60))
          echo "⏰ 早上签到，延迟 $delay 秒后开始.."
          sleep $delay

      - name: 随机延迟（下午/手动触发）
        if: github.event.schedule == '0 5 * * *' || github.event_name == 'workflow_dispatch'
        run: |
          delay=$((RANDOM % 30))
          echo "⏰ 下午兜底或手动触发，延迟 $delay 秒后开始.."
          sleep $delay

      - name: 设置Python环境
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: 安装依赖（新增pytz处理时区）
        run: |
          pip install requests pytz

      - name: 执行签到脚本
        id: sign_script
        env:
          COOKIE_QUARK: ${{ secrets.COOKIE_QUARK }}
          WPUSH_KEY: ${{ secrets.WPUSH_KEY }}
        run: python checkIn_Quark.py

      - name: 确认签到状态（调试用）
        if: always()
        run: |
          echo "是否跳过签到: ${{ needs.check-if-already-signed.outputs.skip }}"
          echo "当前北京时间: $(TZ='Asia/Shanghai' date '+%Y-%m-%d')"
          echo "缓存文件内容: $(cat .last_success_date 2>/dev/null || echo '文件不存在')"
          echo "脚本输出的签到状态: ${{ steps.sign_script.outputs.overall_success }}"

      - name: 清理旧的工作流记录
        uses: Mattraks/delete-workflow-runs@main
        if: always()
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 60
